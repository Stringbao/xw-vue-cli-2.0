#!/usr/bin/env node
const program = require('commander')
const package = require('../package.json');
program.version(package.version, '-v, --version')
	.usage('<command> [项目名称]')
	.command('lev2')
    .command('lev2-init', '参数必填-项目名称')
    .command('lev2-server', '启动Node服务')
	.parse(process.argv)