let routers = [
    {
        path: '/',
        name: 'login',
        component:() => import('@pages/views/login/login.vue')
    },
<%_ data.forEach(function(row){ -%>
    {
        path: "<%= row.path %>",
        name: "<%= row.name %>",
        component:() => import('@pages/views<%= row.componentPath%>')
    },
<%_ _%><% }) -%>
]

export default routers;