let routers = [
    {
        path: '/',
        name: 'login',
        component:() => import('@pages/login/login.vue')
    },
<%_ data.forEach(function(row){ -%>
    {
        path: "<%= row.path %>",
        name: "<%= row.name %>",
        component:() => import('@pages<%= row.componentPath%>')
    },
<%_ _%><% }) -%>
]

export default routers;