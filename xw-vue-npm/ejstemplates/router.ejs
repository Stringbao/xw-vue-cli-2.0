import Vue from 'vue';
import Router from 'vue-router';
Vue.use(Router);

<%_ data.forEach(function(row){ -%>
<%_ row.Pages.forEach(function(rows){ -%>
import <%= row.ModuleName + rows.PageName %> from "@pages/views/<%= row.ModuleName + rows.PageName %>"
<%_ _%><% }) -%>
<%_ _%><% }) -%>
let routers = [
    {
        path: '/',
        name: 'login',
        component:() => import('@pages/login/login.vue')
    },
<%_ data.forEach(function(row){ -%>
    <%_ row.Pages.forEach(function(rows){ -%>
    {
        path:"/<%= row.ModuleName + rows.PageName %>",
        name:"<%= row.ModuleName + rows.PageName %>",
        component:<%= row.ModuleName + rows.PageName %>
    },
    <%_ _%><% }) -%>
<%_ _%><% }) -%>
]
const _router = new Router({
    routes: routers
});

export default _router