<%_ data.imports.forEach(function(row){ -%>
import <%= row.className %> from "./modules/<%= row.filePath %>";
<%_ _%><% }) -%>


export default {
<%_ data.imports.forEach(function(row){ -%>
    <%= row.className %>,
<%_ _%><% }) -%>
}